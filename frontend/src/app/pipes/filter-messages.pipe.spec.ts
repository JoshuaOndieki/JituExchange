import { FilterMessagesPipe } from './filter-messages.pipe';

describe('FilterMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
